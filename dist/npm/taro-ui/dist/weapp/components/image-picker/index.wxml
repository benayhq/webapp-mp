<block>
    <view class="{{rootCls}}" style="{{anonymousState__temp}}">
        <view class="at-image-picker__flex-box" wx:key="{{row.$loopState__temp3}}"
        wx:for="{{loopArray0}}" wx:for-item="row" wx:for-index="i">
            <block wx:for="{{row.$anonymousCallee__2}}" wx:for-item="item" wx:for-index="j"
            wx:key="{{item.$loopState__temp5}}">
                <block wx:if="{{item.$$original.url}}">
                    <view class="at-image-picker__flex-item">
                        <view class="at-image-picker__item">
                            <view class="at-image-picker__remove-btn" bindtap="handleRemoveImg" data-e-handleRemoveImg-so="this"
                            data-e-handleRemoveImg-a-a="{{i * length + j}}"></view>
                            <image class="at-image-picker__preview-img" mode="{{mode}}" src="{{item.$$original.url}}"
                            bindtap="handleImageClick" data-e-handleImageClick-so="this" data-e-handleImageClick-a-a="{{i * length + j}}"
                            />
                        </view>
                    </view>
                </block>
                <block wx:else>
                    <view class="at-image-picker__flex-item" wx:key="{{item.$loopState__temp7}}">
                        <block wx:if="{{item.$$original.type === 'btn'}}">
                            <view class="at-image-picker__item at-image-picker__choose-btn" bindtap="chooseFile">
                                <view class="add-bar"></view>
                                <view class="add-bar"></view>
                            </view>
                        </block>
                    </view>
                </block>
            </block>
        </view>
    </view>
</block>