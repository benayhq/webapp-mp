<block>
    <view class="warpper">
        <view class="layer">
            <view class="header">
                <view>
                    <image style="width:45px;height:45px;margin-left:30px;border-radius: 100px;" __triggerObserer="{{ _triggerObserer }}" src="{{data.inviterProfileUrl}}"></image>
                </view>
                <view class="desc">
                    <view>{{data.inviterName}}</view>
                    <view>邀您参与拼团，仅剩{{data.remainPeople}}个名额</view>
                </view>
            </view>
            <view class="title">{{data.acitivityName}}</view>
            <view class="master">vivi医美咨询师</view>
            <view class="qrCode-detail">长按识别小程序码加入拼团</view>
            <view class="qrCode">
                <image src="{{qrCode}}" __triggerObserer="{{ _triggerObserer }}"></image>
            </view>
        </view>
        <save-image __triggerObserer="{{ _triggerObserer }}"></save-image>
    </view>
</block>