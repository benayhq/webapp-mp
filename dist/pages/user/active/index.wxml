<block wx:if="{{$taroCompReady}}">
    <at-list>
        <block wx:if="{{activeList}}">
            <at-list-item wx:for="{{loopArray11}}" wx:for-item="item" wx:for-index="_anonIdx2" compid="{{item.$compid__57}}"></at-list-item>
        </block>
    </at-list>
</block>