<block wx:if="{{$taroCompReady}}">
    <view>
        <block>
            <block wx:if="{{customer.length === 0}}">
                <empty></empty>
            </block>
            <block wx:else>
                <at-list>
                    <block wx:if="{{customer}}">
                        <block wx:if="{{item.$original.nickname}}" wx:for="{{loopArray59}}" wx:for-item="item" wx:for-index="_anonIdx2">
                            <at-list-item compid="{{item.$compid__182}}"></at-list-item>
                        </block>
                    </block>
                </at-list>
            </block>
        </block>
    </view>
</block>