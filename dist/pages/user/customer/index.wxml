<block>
    <view>
        <block>
            <block wx:if="{{customer.length === 0}}">
                <empty __triggerObserer="{{ _triggerObserer }}"></empty>
            </block>
            <at-list wx:else __triggerObserer="{{ _triggerObserer }}">
                <block wx:if="{{customer}}">
                    <block wx:if="{{item.$$original.name}}" wx:for="{{loopArray0}}" wx:for-item="item">
                        <at-list-item title="{{item.$loopState__temp2}}" __triggerObserer="{{ _triggerObserer }}"
                        note="{{item.$loopState__temp4}}" thumb="{{item.$$original.profileUrl}}"></at-list-item>
                    </block>
                </block>
            </at-list>
        </block>
    </view>
</block>