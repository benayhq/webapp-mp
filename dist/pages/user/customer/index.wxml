<block wx:if="{{$taroCompReady}}">
    <view>
        <block>
            <block wx:if="{{customer.length === 0}}">
                <empty></empty>
            </block>
            <block wx:else>
                <at-list>
                    <block wx:if="{{customer}}">
                        <block wx:if="{{item.$original.name}}" wx:for="{{loopArray2}}" wx:for-item="item" wx:for-index="_anonIdx2">
                            <at-list-item compid="{{item.$compid__46}}"></at-list-item>
                        </block>
                    </block>
                </at-list>
            </block>
        </block>
    </view>
</block>