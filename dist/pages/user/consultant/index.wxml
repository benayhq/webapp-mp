<block>
    <view class="mp-history">
        <item wx:key="{{index}}" __triggerObserer="{{ _triggerObserer }}" active="{{item}}"
        wx:for="{{actives}}" wx:for-item="item" wx:for-index="index"></item>
    </view>
</block>