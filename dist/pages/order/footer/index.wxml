<block wx:if="{{$taroCompReady}}">
    <view class="footer">
        <block wx:if="{{qrCode}}">
            <code compid="{{$compid__74}}"></code>
        </block>
        <info compid="{{$compid__75}}"></info>
    </view>
</block>