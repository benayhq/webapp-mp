<block wx:if="{{$taroCompReady}}">
    <view class="footer">
        <block wx:if="{{qrCode}}">
            <code compid="{{$compid__62}}"></code>
        </block>
        <info compid="{{$compid__63}}"></info>
    </view>
</block>