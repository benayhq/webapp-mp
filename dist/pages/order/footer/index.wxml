<block wx:if="{{$taroCompReady}}">
    <view class="footer">
        <block wx:if="{{qrCode}}">
            <code compid="{{$compid__60}}"></code>
        </block>
        <info compid="{{$compid__61}}"></info>
    </view>
</block>