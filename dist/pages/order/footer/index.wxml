<block wx:if="{{$taroCompReady}}">
    <view class="footer">
        <block wx:if="{{qrCode}}">
            <code compid="{{$compid__59}}"></code>
        </block>
        <info compid="{{$compid__60}}"></info>
    </view>
</block>