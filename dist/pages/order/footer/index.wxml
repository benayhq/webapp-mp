<block>
    <view class="footer">
        <block wx:if="{{qrCode}}"><code content="{{content}}" __triggerObserer="{{ _triggerObserer }}"></code>
        </block>
        <info content="{{content}}" __triggerObserer="{{ _triggerObserer }}"></info>
    </view>
</block>