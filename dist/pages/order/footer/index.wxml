<block wx:if="{{$taroCompReady}}">
    <view class="footer">
        <block wx:if="{{qrCode}}">
            <code compid="{{$compid__61}}"></code>
        </block>
        <info compid="{{$compid__62}}"></info>
    </view>
</block>