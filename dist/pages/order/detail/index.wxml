<block wx:if="{{$taroCompReady}}">
    <view class="mp-order-detail">
        <header compid="{{$compid__29}}"></header>
        <block wx:if="{{content.customerName}}">
            <customer compid="{{$compid__30}}"></customer>
        </block>
        <block wx:if="{{content.batchUsers}}">
            <assemble compid="{{$compid__31}}"></assemble>
        </block>
        <order-product compid="{{$compid__32}}"></order-product>
        <footer compid="{{$compid__33}}"></footer>
        <tool-bar compid="{{$compid__34}}"></tool-bar>
    </view>
</block>