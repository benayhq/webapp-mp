<block wx:if="{{$taroCompReady}}">
    <at-tabs compid="{{$compid__425}}">
        <at-tabs-pane compid="{{$compid__426}}">
            <view>
                <order-item compid="{{$compid__427}}"></order-item>
                <block wx:if="{{list.length > 0}}">
                    <view class="mp-order-loadmore">
                        <at-load-more compid="{{$compid__428}}"></at-load-more>
                    </view>
                </block>
            </view>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__429}}">
            <scroll-view scroll-y="{{true}}" bindscrolltoupper="loadOrderList" style="{{anonymousState__temp}}">
                <block wx:if="{{list}}">
                    <view>
                        <order-item compid="{{$compid__430}}"></order-item>
                        <block wx:if="{{list.length > 0}}">
                            <view class="mp-order-loadmore">
                                <at-load-more compid="{{$compid__431}}"></at-load-more>
                            </view>
                        </block>
                    </view>
                </block>
            </scroll-view>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__432}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__433}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__434}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__435}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__436}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__437}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__438}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__439}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__440}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
    </at-tabs>
</block>