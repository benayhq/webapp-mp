<block wx:if="{{$taroCompReady}}">
    <at-tabs compid="{{$compid__489}}">
        <at-tabs-pane compid="{{$compid__490}}">
            <view>
                <order-item compid="{{$compid__491}}"></order-item>
                <block wx:if="{{list.length > 0}}">
                    <view class="mp-order-loadmore">
                        <at-load-more compid="{{$compid__492}}"></at-load-more>
                    </view>
                </block>
            </view>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__493}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__494}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__495}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__496}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__497}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__498}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__499}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__500}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__501}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__502}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__503}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__504}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
    </at-tabs>
</block>