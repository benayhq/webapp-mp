<block wx:if="{{$taroCompReady}}">
    <at-tabs compid="{{$compid__344}}">
        <at-tabs-pane compid="{{$compid__345}}">
            <view>
                <order-item compid="{{$compid__346}}"></order-item>
                <block wx:if="{{list.length > 0}}">
                    <view class="mp-order-loadmore">
                        <at-load-more compid="{{$compid__347}}"></at-load-more>
                    </view>
                </block>
            </view>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__348}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__349}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__350}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__351}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__352}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__353}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__354}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__355}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__356}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__357}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__358}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__359}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
    </at-tabs>
</block>