<block wx:if="{{$taroCompReady}}">
    <at-tabs compid="{{$compid__1623}}">
        <at-tabs-pane compid="{{$compid__1624}}">
            <view>
                <order-item compid="{{$compid__1625}}"></order-item>
                <block wx:if="{{list.length > 0}}">
                    <view class="mp-order-loadmore">
                        <at-load-more compid="{{$compid__1626}}"></at-load-more>
                    </view>
                </block>
            </view>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__1627}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__1628}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__1629}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__1630}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__1631}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__1632}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__1633}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__1634}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__1635}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__1636}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__1637}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__1638}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
    </at-tabs>
</block>