<block wx:if="{{$taroCompReady}}">
    <at-tabs compid="{{$compid__30}}">
        <at-tabs-pane compid="{{$compid__31}}">
            <view>
                <order-item compid="{{$compid__32}}"></order-item>
                <block wx:if="{{list.length > 0}}">
                    <view class="mp-order-loadmore">
                        <at-load-more compid="{{$compid__33}}"></at-load-more>
                    </view>
                </block>
            </view>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__34}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__35}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__36}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__37}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__38}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__39}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__40}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__41}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__42}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__43}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__44}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__45}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
    </at-tabs>
</block>