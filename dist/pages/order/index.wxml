<block wx:if="{{$taroCompReady}}">
    <at-tabs compid="{{$compid__29}}">
        <at-tabs-pane compid="{{$compid__30}}">
            <view>
                <order-item compid="{{$compid__31}}"></order-item>
                <block wx:if="{{list.length > 0}}">
                    <view class="mp-order-loadmore">
                        <at-load-more compid="{{$compid__32}}"></at-load-more>
                    </view>
                </block>
            </view>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__33}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__34}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__35}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__36}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__37}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__38}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__39}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__40}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__41}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
        <at-tabs-pane compid="{{$compid__42}}">
            <block wx:if="{{list}}">
                <view>
                    <order-item compid="{{$compid__43}}"></order-item>
                    <block wx:if="{{list.length > 0}}">
                        <view class="mp-order-loadmore">
                            <at-load-more compid="{{$compid__44}}"></at-load-more>
                        </view>
                    </block>
                </view>
            </block>
        </at-tabs-pane>
    </at-tabs>
</block>