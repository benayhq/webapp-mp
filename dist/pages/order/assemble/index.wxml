<block wx:if="{{$taroCompReady}}">
    <view class="ping"><text class="mp-icon mp-icon-order-card"></text>
        <view>
            <view class="text"></view>
            <view class="group">
                <block wx:if="{{content.batchUsers}}">
                    <image style="height:50px;width:50px;border-radius:100px;" src="{{item.profileUrl}}" wx:for="{{content.batchUsers}}" wx:for-item="item" wx:for-index="key"></image>
                </block>
            </view>
        </view>
    </view>
</block>