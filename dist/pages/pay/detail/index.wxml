<block>
    <view class="mp-pay">
        <view class="mp-pay-result">
            <view class="mp-icon mp-icon-success"></view>
        </view>
        <view class="mp-pay-detail">交易成功! 立即邀请好友！</view>
        <view class="mp-pay-action" bindtap="handleInvertFirend"
        data-e-handleInvertFirend-so="this">
            <view>邀请好友拼单</view>
        </view>
        <share isOpened="{{isOpened}}" __triggerObserer="{{ _triggerObserer }}"
        path="{{anonymousState__temp}}" activeId="{{activeId}}" activityName="{{activityName}}"
        referId="{{referId}}" img="{{img}}" bindonclose="handleClose" __fn_onClose="{{true}}"></share>
    </view>
</block>